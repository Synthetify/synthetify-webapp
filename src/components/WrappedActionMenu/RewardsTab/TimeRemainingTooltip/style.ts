import { makeStyles } from '@material-ui/core/styles'

const useStyles = makeStyles(() => ({
  title: {
    fontSize: 16,
    lineHeight: '16px',
    fontWeight: 700,
    marginBottom: 10
  },
  icon: {
    height: 20,
    minWidth: 20
  }
}))

export default useStyles
