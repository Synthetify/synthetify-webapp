import { makeStyles } from '@material-ui/core/styles'

const useStyles = makeStyles(() => ({
  root: {
    width: '100%',
    marginTop: 100,
    marginLeft: 40
  },
  headerRoot: {
    width: '100%'
  },
  switch: {
    marginTop: 120
  },
  content: {
    marginTop: 60,
    marginBottom: 140
  },
  roundBorder: {
    borderRadius: 15
  }
}))

export default useStyles
